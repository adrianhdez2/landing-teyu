---
interface Props {
    className?: string;
}

const { className } = Astro.props;
---

<section
    class:list={[
        "max-w-5xl mx-auto flex flex-col px-3 py-16 md:py-36 items-center",
        className,
    ]}
>
    <slot />
</section>
