export interface About {
    title: string;
    description: string;
    img: string;
}

export interface Benefit {
    title: string;
    img: string;
    tag: string;
    description: string;
    direction: 'left' | 'right'
}